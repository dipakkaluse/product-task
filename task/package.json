{
  "name": "task",
  "version": "1.0.0",
  "description": "Objective:\r Design a Node.js GET API that supports pagination and filtering for fetching product details from a database based on specified parameters. The API should handle various parameters such as current page, page size, order by, order direction, and search by field.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sequelize/core": "^7.0.0-alpha.43",
    "@sequelize/mysql": "^7.0.0-alpha.43",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "http-status": "^2.0.0",
    "joi": "^17.13.3",
    "sequelize": "^6.37.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
